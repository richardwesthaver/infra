;;; ~/.sbclrc --- sbcl user init file -*- mode: common-lisp; -*-
(in-package :cl-user)
#-quicklisp
(let ((quicklisp-init "/usr/local/share/lisp/quicklisp/setup.lisp"))
  (when (probe-file quicklisp-init)
    (load quicklisp-init)))

(require :sb-aclrepl)